---
name: Bug report
about: A tool returns wrong data, errors out, or doesn't match its schema
title: "[bug] "
labels: bug
---

## Which tool?

<!-- The tool name you called (e.g. the exact tool id from tools/list). -->

## What did you call it with?

<!-- The exact arguments you passed. Redact any secrets. -->

```json
{ }
```

## What did you expect?

## What did you get?

<!-- Paste the tool's content text and (if applicable) structuredContent.
     If the tool returned isError: true, include the error message verbatim. -->

## Environment

- Package version (`npm ls <package>` or
  `node -p "require('<package>/package.json').version"`):
- Node version (`node --version`):
- MCP client (Claude Desktop / Claude Code / MCP Inspector / other):
- OS:

## Anything else?

<!-- Stack traces, partial logs, screenshots if it's a rendering issue
     in the consuming client. -->
